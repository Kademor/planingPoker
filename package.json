{
  "name": "root-workspace",
  "private": true,
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel server app",
    "server": "npm --workspace server run dev",
    "app": "npm --workspace app run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "pify": "^6.1.0",
    "supports-color": "^10.2.2"
  }
}
